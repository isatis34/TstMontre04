<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- Theme.AppCompat.Light is not a dialog theme -->
    <!--<style name="DialogTheme" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="background">@color/colorDialog</item>
    </style>-->

    <style name="DatePickerDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@color/dialogBackground</item>
        --<item name="android:textColor">@color/dialogColorSimple</item>
        <item name="colorAccent">@color/dialogColor</item>
        <item name="colorPrimaryDark">@color/dialogColorBlack</item>
        <item name="android:textColorSecondary">@color/dialogColorSimple</item>
        <item name="android:headerBackground">@color/dialogBackground</item>
    </style>
</resources>
